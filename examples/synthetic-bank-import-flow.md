# Synthetic Bank Import Flow

> **SYNTHETIC EXAMPLE — NOT A PRIVATE IMPORT FORMAT OR BANK DATA SET**

```text
supplied bank file
       |
       v
preview before ingestion
       |
       v
deterministic checks for recognizable duplicates and completeness
       |
       v
candidate operations
       |
       v
validation queue where confirmation is needed
```

The private application supports manual import as an MVP workflow, including manual SumUp import. This example does not disclose file layouts, real bank identifiers, provider endpoints, matching rules, or client data.
